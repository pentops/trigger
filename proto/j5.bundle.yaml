---
registry:
  owner: pentops
  name: trigger

options:
  subPackages:
    - name: service
    - name: topic

packages:
  - label: Trigger
    name: o5.trigger.v1

dependencies:
  - registry:
      owner: pentops
      name: j5

  - registry:
      owner: pentops
      name: messaging
